# Porta do servidor HTTP
PORT=3000

# Driver do repositorio: "memory" (padrao, sem banco) ou "postgres"
REPO_DRIVER=memory

# String de conexao usada quando REPO_DRIVER=postgres
DATABASE_URL=postgres://taskflow:taskflow@localhost:5432/taskflow
